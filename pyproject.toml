[project]
name = "srs_worms"
version = "0.0.1"
dependencies = [
    "numpy",
    "pandas",
    "scikit-image",
    "pyyaml",
    "matplotlib",
    "jupyter",
]

[tool.setuptools.packages]
find = {}  # Scan the project directory with the default parameters